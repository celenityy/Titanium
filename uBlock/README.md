# uBlock

This configuration is identical to [what we're using on Phoenix](https://phoenix.celenity.dev/content-blocking) - only difference is we're blocking WebGPU by default *(since we unfortunately can't control this via other mechanisms like we can on Firefox with `dom.webgpu.enabled`)*
